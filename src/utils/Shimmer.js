import shimmer from "./../css/shimmer.css";

const Shimmer = ({ message }) => {
  return (
    <div className="shimmer">
      <div>{message}</div>
    </div>
  );
};
export default Shimmer;
